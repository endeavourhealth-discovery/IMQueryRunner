# Request definition
[request_definition]
r = sub, obj, act

# Policy definition
[policy_definition]
p = sub, obj, act, eft

# Matchers
[matchers]
m = r.sub.owner == p.sub && keyMatch(r.obj, p.obj) && keyMatch(r.act, p.act)

# Policy effect
[policy_effect]
e = some(where (p.eft == allow))
